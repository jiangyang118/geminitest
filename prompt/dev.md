

1. PDF/URL 的真实解析、向量检索
2. Flow 编排（例如一键：摘要→PPT→测验）
3. - 解析增强: 增加 CSV/Excel 结构化导入映射、字幕/音频转写、OCR 管线。
- 体验优化: UI 渲染 Mermaid、来源分组/标星、批量导出（MD/JSON/CSV）。
- Flow 编排: 一键“摘要→报告→PPT→测验”，可保存预设与复用。
- 索引升级: 接入 SQLite/pgvector 并分批批量向量化，支持多工作区与权限。 

下一步建议（选择其一继续）

- Flow 编排: 将“摘要→报告→PPT→测验”组合为可保存的流程（参数预设、批处理、多来源合并），支持一键批量生成。
- 体验提升: 前端渲染 Mermaid（思维导图即时可视化）、来源分组/筛选/标星、导出 JSON/CSV。
- 数据治理: 文档去重、相似段合并、规范切分策略（长度/主题/时间轴），分块元数据（章节/页码/时间码）用于精准引用。
记住你的偏好：预设、模板等可保存复用，支持批量执行。
- 完整执行：Flow 从 摘要→PPT→测验 全链路打通，每步带引用，并支持批量运行。

下一步可选

- 预设升级：支持自定义步骤序列（如 摘要→报告→PPT→闪卡→测验）、字段化参数（题量、难度、受众）。
- 计划任务：后端加入定时/队列（如每日自动对新来源跑某预设）。
- 流式体验：生成流式返回、前端增量渲染。
- 可视化与导出：Mermaid 即时渲染、引用定位（页码/时间码/段落号）、导出 JSON/CSV。

需要我继续：为预设增加可视化步骤编辑器（拖拽排序、参数表单），还是先做计划任务（定时/队列）与运行历史筛选？

启动: npm install（若尚未安装依赖），node apps/server/index.js
- 浏览器:
    - 保存预设 → 选择预设 → 点击“设为默认” → 刷新页面应自动选中。
    - 勾选来源 → 保存为预设 → 选择“逐条来源批跑”或合并 → “运行预设” → 查看批处理与“查看最近任务”。

下一最小增量建议（任选其一）

- 可视化步骤编辑器（低保真版）: 在前端用文本数组编辑 steps（例如 summary, report, slides, quiz），保存到预设。
- Job 详情查看: 点击任务行展开对应输出（steps 文本 + 引用），无需新页面。
- 导出 JSON: Flow/批处理结果增加“导出 JSON”按钮，便于与外部系统对接。
